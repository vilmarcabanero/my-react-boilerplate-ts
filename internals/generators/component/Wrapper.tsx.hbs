/**
* Wrapper
*/

import styled from '@emotion/styled';
import * as Material from '@mui/material';

interface Props {
  children: any;
}

export function Wrapper(props: Props) {
  return (
    <StyledWrapper>
      {props.children}
    </StyledWrapper>
  );
}

const StyledWrapper = styled(Material.Paper)``;
