import React from 'react';
import styled from '@emotion/styled';
import { Container as MuiPaper} from '@mui/material';

interface Props {
  children: React.ReactNode;
}

export function Wrapper(props: Props) {
  return (
    <StyledWrapper>
      {props.children}
    </StyledWrapper>
  );
}


const StyledWrapper = styled(MuiPaper)``;
