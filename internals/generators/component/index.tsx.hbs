/**
* {{ properCase componentName }}
*/

import React from 'react';
// import { useDispatch, useSelector } from 'react-redux';
// import { use{{ parent }}Slice } from '../slice';
// import { select{{ properCase parent }} } from '../slice/selectors';
import * as C from './components'

interface Props {}

export function {{ properCase componentName }}(props: Props) {
  // const actions = use{{ properCase parent }}Slice().actions;
  // const dispatch = useDispatch();
  // const state = useSelector(select{{ properCase parent }});
  return <C.Wrapper>{{ properCase componentName }}</C.Wrapper>;
}
