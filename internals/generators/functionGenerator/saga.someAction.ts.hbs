import { ApiCall } from 'api/ApiCall';
import createAPI from 'api/createAPIWithHeader';
import { ApiResponse } from 'apisauce';
import { call } from 'redux-saga/effects';
//import { {{ camelCase parent }}Actions as actions } from '../slice/_index';

export function* {{ camelCase functionName }}() {
  const api = createAPI();

  const response: ApiResponse<any> = yield call(api.call, ApiCall.{{ camelCase functionName }});
  if (response.ok) {
    
  } else {}
}
