import { takeLatest } from 'redux-saga/effects';
import { {{ camelCase parent }}Actions as actions } from '../slice';
import { someAction } from './someAction';

export function* {{ camelCase parent }}Saga() {
  yield takeLatest(actions.someAction.type, someAction);
  // [INSERT NEW ABOVE] < Needed for generating containers seamlessly
}
