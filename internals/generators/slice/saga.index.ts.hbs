import { takeLatest } from 'redux-saga/effects';
import { {{ camelCase parent }}Actions as actions } from '../slice/_index';
import * as Action from './someAction';

export function* {{ camelCase parent }}Saga() {
  yield takeLatest(actions.someAction.type, Action.someAction);
  // [EXPORT NEW SAGA ABOVE] <
}
