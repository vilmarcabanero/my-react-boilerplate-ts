import { createSelector } from '@reduxjs/toolkit';

import { RootState } from 'types';
import { initialState } from '.';

const selectSlice = (state: RootState) => state.{{ camelCase parent }} || initialState;

export const select{{ properCase parent }} = createSelector(
  [selectSlice],
  state => state,
);
